## 通用技术规则

- **不要保留向后兼容性**。移除已过时的路径，而非添加兼容层、回退或迁移。
- **选择最简洁的实现**，完全满足当前需求。避免投机抽象、配置和间接层。
- **分层构建系统**。从能端到端工作的最小版本开始，在已有可工作的产品之上叠加每个新能力。永远不要用未完成的复杂性换取已有的产品。
- **保持组件模块化**，关注点清晰分离。
- **在能降低整体复杂性或提升可靠性时，优先使用成熟、维护良好的库**。没有明确理由时不要重新实现常见功能。
- **在自己实现或添加新包之前，优先使用项目中已有的依赖**。在未检查其文档和类型之前，不要假设某个库缺少某项能力。
- **做出长期的架构决策**。不要接受只能暂时有效、注定之后要被替换的权宜之计。
